[tool.ruff]
line-length = 100
target-version = "py311"
src = ["scripts", "tests", "ml", "sw", "hw"]

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
